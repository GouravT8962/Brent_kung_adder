his is the Readme file for Generalized N-bit Brent Kung Adder.

The code files are attached in main directory as a .zip file.

it accepts inputs in the format
N = number of bits
a = first operand
b = second operand

and gives output/result on the modelsim window
